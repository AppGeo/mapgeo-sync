<div class='flex flex-col items-center p-4'>
  {{#if @model.config}}
    <ConfigStatus
      @config={{@model.config}}
      @selectNewConfig={{this.selectNewConfig}}
    />
  {{else}}
    <WithoutConfig @selectNewConfig={{this.selectNewConfig}} />
  {{/if}}
</div>